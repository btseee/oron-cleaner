# Core ML / audio
torch>=2.6.0
torchaudio>=2.6.0
torchmetrics[audio]>=1.9.0

# Speech models
# ASR for transcript agreement. bayartsogt/wav2vec2-large-xlsr-mongolian scores
# CER 0.123 median where whisper-large-v3 scores 0.311 on the same clips, at a
# fifth of the parameters -- see oron-tts/docs/phase0-findings.md.
transformers>=4.40.0
silero-vad>=6.2.1
# Forced alignment (torchaudio MMS_FA) is the primary transcript gate. The model
# is trained on romanised text across 1,100+ languages, so Mongolian Cyrillic is
# romanised with uroman first.
uroman>=1.3.1

# Audio processing
torchcodec>=0.3.0
librosa>=0.11.0
soundfile>=0.13.1

# Dataset & HuggingFace
requests>=2.32.0
datasets[audio]>=4.8.5
huggingface_hub>=1.16.1

# Text / evaluation
jiwer>=4.0.0

# Mongolian text normalization. Shared with oron-tts so that the text published
# in the corpus, the text scored for CER, and the text used for training are the
# same string. Pure stdlib -- installing it pulls no extra dependencies.
oron-tts @ file:../oron-tts

# Numerics
numpy>=2.0.0
