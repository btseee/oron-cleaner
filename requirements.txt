# Runtime dependencies, mirroring pyproject's [project.dependencies].
#
# This file existed as an empty placeholder, so `pip install -r requirements.txt`
# was a silent no-op -- every pod bootstrap "installed" nothing and each missing
# package surfaced later as a crash mid-run.
#
# It is not redundant with pyproject. `pip install -e .` resolves the declared
# `oron-tts` dependency from an index, and oron-tts is not published to PyPI --
# it is installed from its own checkout. So the editable install fails on a
# clean machine while this file, which omits it, succeeds.
#
# An earlier version of this comment blamed a `requires-python >= 3.14` floor.
# That was wrong: the floor is and has always been >= 3.12, which the CUDA
# images satisfy. The claim came from a review rather than from the file.
torch>=2.6.0
torchaudio>=2.6.0
torchmetrics[audio]>=1.9.0
transformers>=4.40.0
silero-vad>=6.2.1
uroman>=1.3.1
torchcodec>=0.3.0
librosa>=0.11.0
soundfile>=0.13.1
soxr>=0.5.0
datasets[audio]>=4.8.5
huggingface_hub>=1.16.1
jiwer>=4.0.0
numpy>=2.0.0
pyarrow>=17.0.0
