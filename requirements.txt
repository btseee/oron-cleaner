# Runtime dependencies, mirroring pyproject's [project.dependencies].
#
# This file existed as an empty placeholder, so `pip install -r requirements.txt`
# was a silent no-op -- every pod bootstrap "installed" nothing and each missing
# package surfaced later as a crash mid-run. pyproject cannot stand in for it on
# a deployment target, because it declares requires-python >= 3.14 and the CUDA
# images ship 3.12; `pip install -e .` refuses there before it reads a single
# dependency.
#
# oron-tts is deliberately absent: install it from its own checkout first.
torch>=2.6.0
torchaudio>=2.6.0
torchmetrics[audio]>=1.9.0
transformers>=4.40.0
silero-vad>=6.2.1
uroman>=1.3.1
torchcodec>=0.3.0
librosa>=0.11.0
soundfile>=0.13.1
soxr>=0.5.0
datasets[audio]>=4.8.5
huggingface_hub>=1.16.1
jiwer>=4.0.0
numpy>=2.0.0
pyarrow>=17.0.0
